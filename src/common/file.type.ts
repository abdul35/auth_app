export type ReturnFile = {
    size: number;
    originalName: string;
    type: string;
    extension: string;
}

export type FileToRecord = {
    mimetype: string;
    size: number;
    originalFilename: string;
}